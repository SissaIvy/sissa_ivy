# Feature flag environment variables (Vite only reads vars prefixed with VITE_*)
# Set to true/false/1/0. Omit to accept defaults (psyops & blackops enabled in non-prod builds).
VITE_UI_ENABLE_ACTIONS_PANEL=true
VITE_UI_ENABLE_PSYOPS=true
VITE_UI_ENABLE_BLACKOPS=false
